import styled from 'styled-components';
import { Avatar as MaterialAvatar } from '@material-ui/core';

export const StyledAvatar = styled(MaterialAvatar)`
  margin: 0 16px;
  height: 40px;
  width: 40px;
`;
